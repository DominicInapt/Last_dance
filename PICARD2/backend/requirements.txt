redis>=5.0.0
celery
django
djangorestframework
django-cors-headers
requests
python-dotenv


#Needed for spark to run
# Core dependencies (versions compatible with Python 3.10)
certifi>=2024.0.0
charset-normalizer>=3.0.0
colorama>=0.4.6
docopt>=0.6.2
hdfs>=2.7.3
idna>=3.0
requests>=2.31.0
six>=1.16.0
tqdm>=4.66.0
urllib3>=2.0.0

# Data processing
numpy>=1.26.0,<3.0.0
pandas>=2.0.0,<3.0.0
pyarrow>=14.0.0

# Date/time
python-dateutil>=2.8.0
pytz>=2024.1
tzdata>=2024.1

# Note: pyspark and py4j are installed separately in Dockerfile
# to match the Spark binary version (${SPARK_VERSION})
# Do NOT add pyspark or py4j here to avoid version conflicts